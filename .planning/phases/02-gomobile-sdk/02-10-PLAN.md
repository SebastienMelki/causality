---
phase: 02-gomobile-sdk
plan: 10
type: execute
wave: 6
depends_on: ["02-08"]
files_modified:
  - sdk/android/settings.gradle.kts
  - sdk/android/examples/views-example/build.gradle.kts
  - sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/MainActivity.kt
  - sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/ExampleApplication.kt
  - sdk/android/examples/views-example/src/main/res/layout/activity_main.xml
  - sdk/android/examples/views-example/src/main/res/values/strings.xml
  - sdk/android/examples/views-example/README.md
autonomous: true

must_haves:
  truths:
    - "Views example app compiles and runs"
    - "Example demonstrates SDK initialization in Application class"
    - "Example demonstrates all SDK features (track, identify, flush, reset)"
  artifacts:
    - path: "sdk/android/examples/views-example"
      provides: "Android Views example project"
    - path: "sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/ExampleApplication.kt"
      provides: "SDK initialization example"
      contains: "Causality.initialize"
    - path: "sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/MainActivity.kt"
      provides: "SDK usage examples"
      contains: "Causality.track"
  key_links:
    - from: "sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/ExampleApplication.kt"
      to: "Causality"
      via: "SDK initialization"
      pattern: "Causality.initialize"
---

<objective>
Create Android Views example application demonstrating SDK integration with traditional Views architecture.

Purpose: Example apps serve as integration tests and documentation. This Views-based example covers traditional Android development patterns and shows proper SDK initialization and event tracking.

Output: `sdk/android/examples/views-example/` project demonstrating SDK usage.
</objective>

<execution_context>
@/Users/sebastienmelki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sebastienmelki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gomobile-sdk/02-CONTEXT.md
@.planning/phases/02-gomobile-sdk/02-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Android Views example application</name>
  <files>
    sdk/android/settings.gradle.kts
    sdk/android/examples/views-example/build.gradle.kts
    sdk/android/examples/views-example/src/main/AndroidManifest.xml
    sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/ExampleApplication.kt
    sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/MainActivity.kt
    sdk/android/examples/views-example/src/main/res/layout/activity_main.xml
    sdk/android/examples/views-example/src/main/res/values/strings.xml
    sdk/android/examples/views-example/README.md
  </files>
  <action>
Update **sdk/android/settings.gradle.kts** to include Views example:
```kotlin
rootProject.name = "causality-android-sdk"

include(":causality")
include(":examples:views-example")
// Note: compose-example added in Plan 02-11
```

**sdk/android/examples/views-example/build.gradle.kts:**
```kotlin
plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "io.causality.example.views"
    compileSdk = 34

    defaultConfig {
        applicationId = "io.causality.example.views"
        minSdk = 21
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            isMinifyEnabled = false
            proguardFiles(
                getDefaultProguardFile("proguard-android-optimize.txt"),
                "proguard-rules.pro"
            )
        }
    }

    buildFeatures {
        viewBinding = true
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    implementation(project(":causality"))

    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.appcompat:appcompat:1.6.1")
    implementation("com.google.android.material:material:1.11.0")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.7.0")
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3")
}
```

**sdk/android/examples/views-example/src/main/AndroidManifest.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ExampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Material3.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
```

**sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/ExampleApplication.kt:**
```kotlin
package io.causality.example.views

import android.app.Application
import android.util.Log
import io.causality.Causality
import io.causality.config

class ExampleApplication : Application() {
    override fun onCreate() {
        super.onCreate()

        // Initialize Causality SDK using DSL
        try {
            Causality.initialize(this) {
                apiKey = "your-api-key"  // Replace with actual key
                endpoint = "http://10.0.2.2:8080"  // localhost from emulator
                appId = "views-example"
                debugMode = true
            }
            Log.d(TAG, "Causality SDK initialized")
            Log.d(TAG, "Device ID: ${Causality.deviceId}")
        } catch (e: Exception) {
            Log.e(TAG, "Failed to initialize Causality", e)
        }
    }

    companion object {
        private const val TAG = "ViewsExample"
    }
}
```

**sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/MainActivity.kt:**
```kotlin
package io.causality.example.views

import android.os.Bundle
import android.util.Log
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity
import androidx.lifecycle.lifecycleScope
import io.causality.Causality
import io.causality.event
import io.causality.example.views.databinding.ActivityMainBinding
import kotlinx.coroutines.launch

class MainActivity : AppCompatActivity() {
    private lateinit var binding: ActivityMainBinding

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        binding = ActivityMainBinding.inflate(layoutInflater)
        setContentView(binding.root)

        setupUI()
        trackScreenView()
    }

    private fun setupUI() {
        binding.deviceIdText.text = "Device ID: ${Causality.deviceId}"

        binding.trackEventButton.setOnClickListener {
            trackEvent()
        }

        binding.trackPurchaseButton.setOnClickListener {
            trackPurchase()
        }

        binding.identifyButton.setOnClickListener {
            identifyUser()
        }

        binding.flushButton.setOnClickListener {
            flushEvents()
        }

        binding.resetButton.setOnClickListener {
            resetUser()
        }
    }

    private fun trackScreenView() {
        Causality.trackScreenView("main_activity")
        Log.d(TAG, "Screen view tracked: main_activity")
    }

    private fun trackEvent() {
        // Using DSL builder
        Causality.track("button_tap") {
            property("button_name", "track_event")
            property("screen", "main_activity")
            property("timestamp", System.currentTimeMillis())
        }
        Log.d(TAG, "Event tracked: button_tap")
        showToast("Event tracked!")
    }

    private fun trackPurchase() {
        // Using event DSL
        val purchaseEvent = event("purchase") {
            property("product_id", "premium-subscription")
            property("price", 9.99)
            property("currency", "USD")
            property("quantity", 1)
        }
        Causality.track(purchaseEvent)
        Log.d(TAG, "Event tracked: purchase")
        showToast("Purchase tracked!")
    }

    private fun identifyUser() {
        try {
            Causality.identify(
                userId = "android-user-789",
                traits = mapOf(
                    "name" to "Alex Developer",
                    "email" to "alex@example.com",
                    "tier" to "premium"
                )
            )
            Log.d(TAG, "User identified: android-user-789")
            showToast("User identified!")
        } catch (e: Exception) {
            Log.e(TAG, "Identify error", e)
            showToast("Error: ${e.message}")
        }
    }

    private fun flushEvents() {
        binding.flushButton.isEnabled = false
        binding.flushButton.text = "Flushing..."

        lifecycleScope.launch {
            try {
                Causality.flush()
                Log.d(TAG, "Events flushed")
                showToast("Events flushed!")
            } catch (e: Exception) {
                Log.e(TAG, "Flush error", e)
                showToast("Error: ${e.message}")
            } finally {
                binding.flushButton.isEnabled = true
                binding.flushButton.text = "Flush Events"
            }
        }
    }

    private fun resetUser() {
        try {
            Causality.reset()
            Log.d(TAG, "User reset")
            showToast("User reset!")
        } catch (e: Exception) {
            Log.e(TAG, "Reset error", e)
            showToast("Error: ${e.message}")
        }
    }

    private fun showToast(message: String) {
        Toast.makeText(this, message, Toast.LENGTH_SHORT).show()
    }

    companion object {
        private const val TAG = "ViewsExample"
    }
}
```

**sdk/android/examples/views-example/src/main/res/layout/activity_main.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <TextView
        android:id="@+id/deviceIdText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="?android:attr/textColorSecondary"
        android:layout_marginBottom="24dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/trackEventButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Track Event"
        android:layout_marginBottom="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/trackPurchaseButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Track Purchase"
        android:layout_marginBottom="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/identifyButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Identify User"
        android:layout_marginBottom="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/flushButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Flush Events"
        android:layout_marginBottom="12dp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/resetButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Reset User" />

</LinearLayout>
```

**sdk/android/examples/views-example/src/main/res/values/strings.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="app_name">Causality Views Example</string>
</resources>
```

**sdk/android/examples/views-example/README.md:**
```markdown
# Causality Android Views Example

This example demonstrates integrating the Causality SDK with a traditional Android Views-based app.

## Setup

1. Build the AAR:
   ```bash
   make mobile-android
   ```

2. Open the project in Android Studio:
   ```bash
   cd sdk/android
   # Open in Android Studio
   ```

3. Update the API key and endpoint in `ExampleApplication.kt`

4. Run on emulator or device

## Features Demonstrated

- SDK initialization in Application class
- Screen view tracking
- Button tap tracking with properties
- E-commerce purchase tracking
- User identification with traits
- Coroutine-based flush
- User reset

## Code Patterns

### Initialization in Application
```kotlin
class MyApp : Application() {
    override fun onCreate() {
        super.onCreate()
        Causality.initialize(this) {
            apiKey = "your-api-key"
            endpoint = "https://your-server.com"
            appId = "your-app-id"
        }
    }
}
```

### Event Tracking with DSL
```kotlin
Causality.track("button_tap") {
    property("button_name", "checkout")
    property("price", 29.99)
}
```

### User Identification
```kotlin
Causality.identify(
    userId = "user-123",
    traits = mapOf("email" to "user@example.com")
)
```

### Flush with Coroutines
```kotlin
lifecycleScope.launch {
    Causality.flush()
}
```
```
  </action>
  <verify>
    - Directory exists: `ls sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/`
    - Layout exists: `ls sdk/android/examples/views-example/src/main/res/layout/`
  </verify>
  <done>
    - Views example with Application class initialization
    - MainActivity with View Binding
    - Layout with Material buttons
    - Demonstrates all SDK features
    - README with setup instructions
  </done>
</task>

</tasks>

<verification>
- Views example: `ls sdk/android/examples/views-example/src/main/kotlin/io/causality/example/views/`
- Layout: `ls sdk/android/examples/views-example/src/main/res/layout/`
- Settings updated: `grep "views-example" sdk/android/settings.gradle.kts`
- (Full build requires AAR from gomobile)
</verification>

<success_criteria>
- Views example demonstrates Application initialization
- Views example uses View Binding and Material components
- Example shows event tracking, identification, flush, reset
- README has setup instructions
</success_criteria>

<output>
After completion, create `.planning/phases/02-gomobile-sdk/02-10-SUMMARY.md`
</output>
